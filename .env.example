# 📋 Environment Variables Template
# Copy this file to .env and fill in your actual values

# Google Gemini API Configuration (REQUIRED)
GOOGLE_API_KEY=your_google_gemini_api_key_here

# Vector Database Configuration (REQUIRED for Vector DB features)
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_ENVIRONMENT=us-west1-gcp
PINECONE_INDEX_NAME=call-centre-tickets

# OpenAI API Configuration (REQUIRED for embeddings)
OPENAI_API_KEY=your_openai_api_key_here

# Model Configuration (Optional - has defaults)
OTHER_CATEGORY_THRESHOLD=0.6
ENSEMBLE_WEIGHT=0.7

# Demo Configuration (Optional - has defaults)
DEMO_PORT=8502
DEMO_HOST=localhost

# Vector DB Health Check API (Optional - has defaults)
VECTOR_HEALTH_PORT=8080
VECTOR_HEALTH_HOST=0.0.0.0

# Logging Configuration (Optional - has defaults)
LOG_LEVEL=INFO

# Instructions:
# 1. Get your Google Gemini API key from: https://aistudio.google.com/
# 2. Get your Pinecone API key from: https://www.pinecone.io/
# 3. Get your OpenAI API key from: https://platform.openai.com/api-keys
# 4. Copy this file: cp .env.example .env
# 5. Replace all 'your_*_api_key_here' with your actual API keys
# 6. Adjust other settings as needed

# Note: Vector database features are optional - the system will work
# without them but with reduced RAG (Retrieval-Augmented Generation) capabilities